{% extends 'base.html.twig' %}

{% block title %}Producto {{product.id}} {% endblock %}

{% block body %}

<div class="row">
	<div class="col s12 l6 offset-l3" style="margin-top:40px">
		<div class="card ">
        <div class="card-content">
          <span class="card-title"><b>{{ product.nombre }}</b></span>
          <p>{{ product.descripcion }}</p>
		<p><b>$ {{ product.precio }}</b></p>
        </div>
        <div class="card-action">
          <a href="#">This is a link</a>
          <a href="#">This is a link</a>
        </div>
      </div>
		
	</div>
</div>

{% endblock %}